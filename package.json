{
  "name": "faceoff",
  "version": "1.0.0",
  "description": "Face-Off Quiz — pick the winner in side-by-side comparisons",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "openai": "^4.0.0"
  }
}
